package com.descenty.work_in_spring.user.service;

import java.net.URI;
import java.net.http.HttpRequest;
import java.util.Optional;
import java.util.UUID;

import org.springframework.stereotype.Service;

import com.descenty.work_in_spring.user.dto.UserCreate;

@Service
public class UserService {
    // @Value("${KC_HOST}")
    // private String keycloak_host;
    // private final String kcRealm = "workin";

    // public Optional<UUID> create(UserCreate userCreate) {
    //     HttpRequest HttpRequest = java.net.http.HttpRequest
    //             .newBuilder(URI.create(issuerUri + "/users"))
    // }
}
